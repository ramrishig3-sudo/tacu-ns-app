[providers]
python = {}

[phases.setup]
nixPkgs = ["python3", "pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "uvicorn backend_blueprint:app --host 0.0.0.0 --port $PORT"
